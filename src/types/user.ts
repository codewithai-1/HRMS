export interface User {
  id: string;
  employeeId: string;
  firstName: string;
  lastName: string;
  email: string;
  departmentId: string;
  role: string;
  isActive: boolean;
  createdAt: string;
  updatedAt: string;
} 